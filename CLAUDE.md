# CLAUDE.md

@AGENTS.md

## Notas específicas do Claude Code

- Agentes especialistas do projeto estão em `.claude/agents/` — use o agente adequado à área da tarefa (migração, segurança, frontend, dados, testes, docs).
- Antes de implementar feature nova, verifique se existe spec em `docs/specs/`; se não existir, crie uma a partir de `docs/specs/template/`.
- Ao alterar arquitetura, atualize `docs/architecture/c4/` e registre a decisão em `docs/adr/`.
